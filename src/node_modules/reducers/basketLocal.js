import * as R from 'ramda'

import {
ADD_PHONE_TO_BASKET,
DEL_PHONE_TO_BASKET,
REMOVE_FROM_PHONE_BASKET,
CLEAN_BASKET
} 
from 'actionTypes'

const initialState = []

export default (state = initialState,  {type,payload,id}) => {
    switch (type) {
        case ADD_PHONE_TO_BASKET:
            const add =id = R.compose(
                R.filter(basketId => basketId.id === 1),
                R.map(id => id)
            )(payload)
        
             return R.append(add,state)
           
        // case REMOVE_FROM_PHONE_BASKET:
        //     return R.without(R.of(payload),state)
        //  case DEL_PHONE_TO_BASKET:
        //     const index = payload.indexOf(id);
        //     if (index > -1) {
        //          payload.splice(index, 1);
        //     }
        //     return R.without(R.of(payload),state)
        //     case CLEAN_BASKET:
        //     return initialState
        default:
            return state
    }
}