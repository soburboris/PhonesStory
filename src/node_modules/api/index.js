import phones from './mockPhones'
import categories from 'api/mockCatgories'
// import request from 'superagent'
import * as R from 'ramda'


// export const fetchPhones = async () => {
//     const {body} = await request.get(
//       'https://run.mocky.io/v3/773458a3-884a-4b61-ba6e-8f2c94084913?'
//       // 'http://localhost:3012/'
//     )
//     return body.phones
//   }

export const fetchPhones = async () => {
  return new Promise(resolve => {
    resolve(phones)
  })
}




// export const loadMorePhones = async ({offset}) => {
//     const {body} = await request.get(
//         `https://run.mocky.io/v3/773458a3-884a-4b61-ba6e-8f2c94084913?offset=${offset}`
//         // 'http://127.0.0.1:3012/'
//       )
//       return body.phones

// }

export const loadMorePhones = async ({ offset }) => {
  return new Promise(resolve => {
    resolve(phones)
  })
}

// export const fetchPhonesById = async (id) => {
//   const {body} = await request.get(
//     'http://www.mocky.io/v2/5918b9461200001f1040dbeb'
//     // 'http://localhost:3012/'
//   )

//   return  R.find(R.propEq('id',id),body.phones)
//   }
export const fetchPhonesById = async (id) => {
  return new Promise(resolve => {
    resolve(R.find(R.propEq('id', id), phones))
  })
}

// const idtest =body.phones.filter(elem =>elem.id === id)
// idtest[idtest.findIndex(elem =>elem.id === id )]


export const fetchCategories = async () => {
  return new Promise(resolve => {
    resolve(categories)
  })
}
